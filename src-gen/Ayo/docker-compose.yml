# WinVMJ Generated Docker Compose
# Product: Koperasibodong

version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ayo-postgres
    environment:
      POSTGRES_DB: ayo
      POSTGRES_USER: ${AMANAH_DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${AMANAH_DB_PASSWORD:-postgres123}
    ports:
      - "${HOST_PORT_POSTGRES:-15006}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - ayo-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ayo-backend
    environment:
      AMANAH_HOST_BE: ${AMANAH_HOST_BE:-0.0.0.0}
      AMANAH_PORT_BE: ${AMANAH_PORT_BE:-7776}
      # Internal ports are fixed (7776, 9464) - configured in Dockerfile
      AMANAH_DB_URL: ${AMANAH_DB_URL:-jdbc:postgresql://postgres:5432/ayo}
      AMANAH_DB_USERNAME: ${AMANAH_DB_USERNAME:-postgres}
      AMANAH_DB_PASSWORD: ${AMANAH_DB_PASSWORD:-postgres123}
    ports:
      # HOST_PORT:CONTAINER_PORT (container port fixed at 7776)
      - "${HOST_PORT_BE:-15005}:7776"
    networks:
      - ayo-network
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

networks:
  ayo-network:
    driver: bridge

volumes:
  postgres_data:
